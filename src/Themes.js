import { createTheme } from '@material-ui/core'

export const mainTheme = createTheme({
    palette: {
        primary: {
            main: '#5698bd',
        },
        secondary: {
            main: '#E1D7C6',
        },
    },
})
